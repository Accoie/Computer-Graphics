<Window x:Class="Task3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Окружность с настраиваемой толщиной" Width="850" Height="700"
        WindowStartupLocation="CenterScreen">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#f0f0f0" Padding="10" BorderBrush="#ccc" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Толщина обводки:" Margin="0,0,10,0" FontWeight="Bold"/>
                <Slider x:Name="thicknessSlider" Minimum="1" Maximum="10" Width="200" 
                        TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,10,0"/>
                <TextBlock x:Name="thicknessValueText" Width="30" TextAlignment="Center" FontWeight="Bold"/>
                <TextBlock Text="пикс."/>
            </StackPanel>
        </Border>

        <Canvas x:Name="canvas" Grid.Row="1" Background="White"/>
    </Grid>
</Window>